{
  "name": "vinternet-music-vault-desktop",
  "version": "1.0.0",
  "description": "Desktop application for Vinternet Music Vault (Electron)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thevinternet/vinternet-music-vault-desktop.git"
  },
  "author": "James Vinter",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/thevinternet/vinternet-music-vault-desktop/issues"
  },
  "main": "electron.js",
  "scripts": {
    "development": "concurrently -k \"cd client && yarn start\" \"cd server && yarn development\" \"yarn:electron\"",
    "electron": "wait-on http://localhost:3000 && electron ."
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "electron": "^13.1.7",
    "electron-devtools-installer": "^3.2.0",
    "electron-is-dev": "^2.0.0",
    "wait-on": "^6.0.0"
  }
}
